{
  "name": "nest-js-api-production-with-ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:push": "pnpm --dir api run db:push",
    "db:studio": "pnpm --dir api run db:studio",
    "dev:api": "pnpm --filter api run start:dev",
    "dev:mobile": "pnpm --filter client run dev",
    "dev": "concurrently -n \"API,EXPO\" -c \"blue.bold,green.bold\" \"pnpm dev:api\" \"pnpm dev:mobile\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.4.2",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@12.4.2",
  "type": "module",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
